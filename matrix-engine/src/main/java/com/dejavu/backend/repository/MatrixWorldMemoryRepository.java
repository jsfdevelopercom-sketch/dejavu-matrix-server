package com.dejavu.backend.repository;

import com.dejavu.backend.model.MatrixWorldMemory;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MatrixWorldMemoryRepository extends JpaRepository<MatrixWorldMemory, Long> {
}
